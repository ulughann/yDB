// on docuemnt load
document.addEventListener("DOMContentLoaded", function(event) {
    // get element with class "cover show"
    var cover = document.getElementsByClassName("cover show")[0];
    // set style to nothing
    cover.style = "background-color: green";
});

    